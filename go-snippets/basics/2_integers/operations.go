package integers

// The function below is "commented", so it has "documentation". Is not a really useful comment,
// because you can just infer it from the name of the function.

// Add takes two integers and returns the sum of them.
func Add(a, b int) int {
	return a + b
}
